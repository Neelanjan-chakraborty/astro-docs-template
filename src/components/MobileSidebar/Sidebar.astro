---
import LeftSidebar from '../LeftSidebar/LeftSidebar.astro';

type Props = {
  isSidebarOpen: boolean;
  currentPage: string;
};

const { isSidebarOpen, currentPage } = Astro.props as Props;
---

</script>

<div class="sidebar-shadow" onclick="toggleSidebar()"></div>
<nav class={`sidebar ${isSidebarOpen ? 'open' : ''}`}>
    <LeftSidebar currentPage={currentPage} />
</nav>

<style>
.sidebar-shadow {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  backdrop-filter: blur(10px);
  z-index: 90;
  display: none;
  overflow: hidden;
}

.sidebar-shadow.active {
  display: block;
}

.sidebar {
  position: fixed;
  top: 0;
  left: -75%;
  width: 75%;
  max-width: 300px;
  height: 100%;
  box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.5), 0 4px 6px -4px rgb(0 0 0 / 0.5);
  background: var(--theme-bg);
  backdrop-filter: blur(10px);
  transition: left 0.3s ease-in-out;
  z-index: 95;
  overflow-y: auto;
}

.sidebar.open {
  left: 0;
}
</style>
